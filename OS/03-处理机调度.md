---

# 🖥️ 操作系统课程笔记
## 第3章 处理机调度（Process Scheduling）

---

### 📚 本章目录
- [3.1 调度的基本概念](#31-调度的基本概念)
- [3.2 调度层次](#32-调度层次)
- [3.3 调度准则](#33-调度准则)
- [3.4 典型调度算法](#34-典型调度算法)
- [3.5 多处理器调度简介](#35-多处理器调度简介)

---

## 3.1 调度的基本概念
### 3.1.1 定义
处理机调度是指**按照某种算法，从就绪队列中选取进程，并将处理机分配给它**的过程。
核心目的：**提高系统吞吐量、减少响应时间、公平分配资源**。

### 3.1.2 调度的本质
- 处理机资源是**唯一且稀缺**的
- 多进程并发运行时，CPU 必须在进程间快速切换
- 调度决定了**谁能占用 CPU、占用多久**

---

## 3.2 调度层次
操作系统中通常有三个调度层级：

### 3.2.1 高级调度（作业调度）
- **作用**：从外存作业池中选中作业调入内存
- **发生频率**：低（分钟级）
- **主要任务**：创建进程、分配资源
- **考点**：吞吐量是主要指标

### 3.2.2 中级调度（内存调度）
- **作用**：实现**虚拟内存**与进程换入换出
- **目的**：提高内存利用率，平衡系统负载
- **发生频率**：中

### 3.2.3 低级调度（进程调度）
- **作用**：决定就绪队列中哪个进程获得 CPU
- **发生频率**：高（毫秒级）
- **核心**：直接影响系统性能
- **必考**：本章重点

---

## 3.3 调度准则（评价指标）
评价调度算法优劣的 5 大标准：

1. **周转时间**（Turnaround Time）
   - 定义：从作业提交到完成的时间
   - 越小越好

2. **带权周转时间**
   - 定义：周转时间 / 服务时间
   - 越接近 1 越好（说明基本等待为0）

3. **响应时间**（Response Time）
   - 定义：从提交到首次获得 CPU 的时间
   - 交互式系统关键指标

4. **吞吐量**（Throughput）
   - 单位时间完成的作业数
   - 越大越好

5. **CPU 利用率**
   - CPU 忙碌时间比例
   - 越高越好

---

## 3.4 典型调度算法
### 3.4.1 先来先服务 FCFS（First-Come, First-Served）
**算法思想**：按到达顺序分配 CPU
**特点**：
- 公平、简单、无饥饿
- 对短作业不利（短作业被迫等长作业）
- 平均周转时间较长

**公式**：
- 等待时间 = 前一作业完成时间 - 本作业到达时间
- 周转时间 = 完成时间 - 到达时间

---

### 3.4.2 短作业优先 SJF（Shortest Job First）
**算法思想**：选择**预计运行时间最短**的进程
**优点**：
- 平均周转时间最小
- 效率高
**缺点**：
- 长作业“饥饿”（可能一直得不到执行）
- 需要预知运行时间（实际难以做到）

---

### 3.4.3 短进程优先 SPF（Preemptive SJF）
**算法思想**：SJF 的抢占版
- 新进程到达时，若其运行时间 < 当前进程剩余时间 → **强制抢占 CPU**

**特点**：
- 响应时间更优
- 对长作业不友好

---

### 3.4.4 时间片轮转 RR（Round Robin）
**算法思想**：
- 每个进程分配一个固定时间片（quantum）
- 时钟中断到时，强制切换到下一个进程
- 适用于**分时系统 / 操作系统**

**关键参数**：
- 时间片大小 = 关键
  - 太大 → 退化为 FCFS
  - 太小 → 切换开销大

**公式**：
- 响应时间 ≈ 时间片 × （进程数 - 1）

---

### 3.4.5 优先级调度（Priority Scheduling）
**算法思想**：
- 每个进程对应一个优先级（数字越小优先级越高）
- 每次选择优先级最高的进程

**分类**：
- **非抢占式**：运行直到结束
- **抢占式**：更高优先级进程到达时抢占 CPU

**问题**：低优先级进程“饥饿”
→ 解决：**老化（Aging）技术**：随时间提高低优先级进程优先级

---

### 3.4.6 多级反馈队列（Multilevel Feedback Queue）
**操作系统中最实用的算法！**（必考）

**机制**：
1. 多个队列，优先级从高到低
2. 时间片随队列等级降低而增大（如：第1级 1ms，第2级 2ms，第3级 4ms...）
3. 新进程进入第1级队列
4. 未执行完则降级到下一级队列
5. 队列内采用 RR 或 FCFS

**优点**：
- 短作业：快速完成（高优先级队列）
- 长作业：公平执行（降级队列）
- 交互式任务响应快（高优先级）

---

## 3.5 多处理器调度简介
### 3.5.1 调度挑战
- 负载均衡（避免某一 CPU 空闲）
- 减少缓存失效（Cache Affinity）
- 同步与互斥开销

### 3.5.2 常见方式
1. **统一调度队列（Unix/Linux）**
   - 所有 CPU 共享一个就绪队列
   - 简单但存在锁竞争

2. **私有队列（Windows）**
   - 每个 CPU 有自有队列
   - 负载低时迁移任务

---

## ✅ 本章必背核心考点总结
1. **三级调度**：高级、中级、低级（必考）
2. **五大评价准则**：周转时间、响应时间、吞吐量、CPU 利用率、公平性
3. **四大算法对比**：
   - FCFS：对短作业不友好
   - SJF：最优平均周转时间，但长作业饥饿
   - RR：分时系统标准，响应快
   - 多级反馈队列：OS 实际采用的综合最优算法

---
